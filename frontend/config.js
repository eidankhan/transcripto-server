/**
 * @author Eidan Khan
 */
const CONFIG = {
    // LOCAL DEVELOPMENT (Fedora)
    API_BASE_URL: "http://localhost/api",
    
    // PRODUCTION (Uncomment when deploying to transcripto.dev)
    // PI_BASE_URL: "https://api.transcripto.dev",
    
    VERSION: "1.0.0"
};
